{
  "name": "strapi-next-base",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "generate:component": "turbo run generate:component --filter=ui"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.16",
    "eslint": "^8.46.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.11.0",
  "engines": {
    "node": "21.3.0"
  }
}
